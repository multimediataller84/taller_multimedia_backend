export type TConfig = {
  DATABASE: string;
  USER: string;
  PASSWORD: string;
  HOST: string;
  JWT_ACCESS_SECRET: string;
  NODE_ENV: string;
  CORS_ORIGIN: string;
  PORT: string;
  URL: string;
  DB_PORT: string;
};
